version: 3
projects:
  - dir: authentik-test
    autoplan:
      when_modified: ["*.tf", "*.tfvars", "*.tfvars.json"]
      enabled: true
    terraform_extra_args:
      - name: var-file
        arguments:
          - "-var-file=user.tfvars.json"
